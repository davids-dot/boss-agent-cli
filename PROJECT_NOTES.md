# boss-agent-cli · 经验指针

> ⚠️ 本文件**不是经验正文**，只是一条指针。
> 本机**唯一的公共经验区是 `~/.memory/`**；各 agent 自己的记忆文件各自负责、互不修改。

## 去哪找

| 内容 | 位置 |
|---|---|
| 索引总览（**先读这个**） | `~/.memory/README.md` |
| 本工具完整经验（基本信息 / 命令速查 / 已知问题与解法） | `~/.memory/boss-agent-cli.md` |
| 用户个人信息、求职期望、常用语 | `~/.memory/me.md` |
| 每日事件与踩坑 | `~/.memory/daily-events` |
| 可复用脚本 | `~/.memory/scripts/` |
| 凭据位置 | `~/.memory/README.md` →「凭据」一节 |
| 项目级经验（**Hermes 专用，只读**） | `./AGENTS.md` —— 其他 agent 不得修改 |

## 约定
- 完成有跨会话价值的任务后，把要点追加到 `~/.memory/daily-events`；
  工具相关的细节请更新 `~/.memory/boss-agent-cli.md`，**不要在本文件里堆正文**。
- 不要创建第二个「公共经验区」。共享的东西一律进 `~/.memory/`。
